(function($, window) {
	/*	var cb, buildList, prepareBlog;
	prepareBlog = function() {
		var json = $.getJSON('res/blog.json');
		return json.then(function(data) {
			sf.blogData = data;
		});
	};
	buildList = function() {

	};

	cb = function() {
		var dfr = $.Deferred();
		$.extend(
			sf, {
				prepareBlog: prepareBlog,
				buildList: buildList,
			});
		dfr.resolve(prepareBlog());
	};
	return cb(); */

}(jQuery, window));